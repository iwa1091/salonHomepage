[mysqld]
# 文字コード（日本語/絵文字のため utf8mb4 推奨）
character-set-server = utf8mb4
collation-server     = utf8mb4_unicode_ci

# タイムゾーン（日本）
default-time-zone = '+09:00'

# 必要ならログ等ここに追記できます
# general_log = 0
# slow_query_log = 0

[client]
default-character-set = utf8mb4

[mysql]
default-character-set = utf8mb4
